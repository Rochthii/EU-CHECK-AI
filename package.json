{
  "name": "eu-check-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "eu-check-web"
  ],
  "scripts": {
    "dev": "npm run dev --prefix eu-check-web",
    "build": "npm run build --prefix eu-check-web",
    "start": "npm run start --prefix eu-check-web",
    "lint": "npm run lint --prefix eu-check-web"
  }
}
